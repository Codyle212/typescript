{
    "name": "web",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "start:db": "json-server -w db.json",
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview"
    },
    "devDependencies": {
        "typescript": "^4.6.4",
        "vite": "^3.2.3"
    },
    "dependencies": {
        "axios": "^1.1.3"
    }
}
